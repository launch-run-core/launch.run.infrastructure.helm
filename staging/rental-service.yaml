apiVersion: apps/v1
kind: Deployment
metadata:
  name: rental-service
  namespace: forja-staging
  labels:
    app: rental-service
    app.kubernetes.io/part-of: forja
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rental-service
  template:
    metadata:
      labels:
        app: rental-service
        app.kubernetes.io/part-of: forja
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: rental-service
          image: ghcr.io/forja-platform/forja-rental-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9021
              protocol: TCP
          envFrom:
            - configMapRef:
                name: forja-config
            - secretRef:
                name: forja-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 9021
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 9021
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: rental-service
  namespace: forja-staging
  labels:
    app: rental-service
    app.kubernetes.io/part-of: forja
spec:
  type: ClusterIP
  selector:
    app: rental-service
  ports:
    - name: http
      port: 9021
      targetPort: 9021
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: rental-service
  namespace: forja-staging
  labels:
    app: rental-service
    app.kubernetes.io/part-of: forja
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: rental-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
