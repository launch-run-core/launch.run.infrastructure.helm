apiVersion: apps/v1
kind: Deployment
metadata:
  name: feed-service
  namespace: launch-run-dev
  labels:
    app: feed-service
    app.kubernetes.io/part-of: launch-run
spec:
  replicas: 2
  selector:
    matchLabels:
      app: feed-service
  template:
    metadata:
      labels:
        app: feed-service
        app.kubernetes.io/part-of: launch-run
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: feed-service
          image: ghcr.io/launch-run-core/launch.run.feed-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9037
              protocol: TCP
          envFrom:
            - configMapRef:
                name: launch-run-config
            - secretRef:
                name: launch-run-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 9037
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 9037
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: feed-service
  namespace: launch-run-dev
  labels:
    app: feed-service
    app.kubernetes.io/part-of: launch-run
spec:
  type: ClusterIP
  selector:
    app: feed-service
  ports:
    - name: http
      port: 9037
      targetPort: 9037
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: feed-service
  namespace: launch-run-dev
  labels:
    app: feed-service
    app.kubernetes.io/part-of: launch-run
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: feed-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
