apiVersion: apps/v1
kind: Deployment
metadata:
  name: feature-flag-service
  namespace: forja-staging
  labels:
    app: feature-flag-service
    app.kubernetes.io/part-of: forja
spec:
  replicas: 2
  selector:
    matchLabels:
      app: feature-flag-service
  template:
    metadata:
      labels:
        app: feature-flag-service
        app.kubernetes.io/part-of: forja
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: feature-flag-service
          image: ghcr.io/forja-platform/forja-feature-flag-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9026
              protocol: TCP
          envFrom:
            - configMapRef:
                name: forja-config
            - secretRef:
                name: forja-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 9026
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 9026
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: feature-flag-service
  namespace: forja-staging
  labels:
    app: feature-flag-service
    app.kubernetes.io/part-of: forja
spec:
  type: ClusterIP
  selector:
    app: feature-flag-service
  ports:
    - name: http
      port: 9026
      targetPort: 9026
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: feature-flag-service
  namespace: forja-staging
  labels:
    app: feature-flag-service
    app.kubernetes.io/part-of: forja
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: feature-flag-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
