apiVersion: apps/v1
kind: Deployment
metadata:
  name: media-processing
  namespace: launch-run-dev
  labels:
    app: media-processing
    app.kubernetes.io/part-of: launch-run
spec:
  replicas: 2
  selector:
    matchLabels:
      app: media-processing
  template:
    metadata:
      labels:
        app: media-processing
        app.kubernetes.io/part-of: launch-run
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: media-processing
          image: ghcr.io/launch-run-core/launch.run.media-processing:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9054
              protocol: TCP
          envFrom:
            - configMapRef:
                name: launch-run-config
            - secretRef:
                name: launch-run-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 9054
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 9054
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: media-processing
  namespace: launch-run-dev
  labels:
    app: media-processing
    app.kubernetes.io/part-of: launch-run
spec:
  type: ClusterIP
  selector:
    app: media-processing
  ports:
    - name: http
      port: 9054
      targetPort: 9054
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: media-processing
  namespace: launch-run-dev
  labels:
    app: media-processing
    app.kubernetes.io/part-of: launch-run
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: media-processing
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
