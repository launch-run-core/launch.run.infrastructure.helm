apiVersion: apps/v1
kind: Deployment
metadata:
  name: kyc-service
  namespace: forja-staging
  labels:
    app: kyc-service
    app.kubernetes.io/part-of: forja
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kyc-service
  template:
    metadata:
      labels:
        app: kyc-service
        app.kubernetes.io/part-of: forja
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: kyc-service
          image: ghcr.io/forja-platform/forja-kyc-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9043
              protocol: TCP
          envFrom:
            - configMapRef:
                name: forja-config
            - secretRef:
                name: forja-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 9043
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 9043
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: kyc-service
  namespace: forja-staging
  labels:
    app: kyc-service
    app.kubernetes.io/part-of: forja
spec:
  type: ClusterIP
  selector:
    app: kyc-service
  ports:
    - name: http
      port: 9043
      targetPort: 9043
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: kyc-service
  namespace: forja-staging
  labels:
    app: kyc-service
    app.kubernetes.io/part-of: forja
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: kyc-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
