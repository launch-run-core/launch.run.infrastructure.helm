apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction-service
  namespace: launch-run-dev
  labels:
    app: transaction-service
    app.kubernetes.io/part-of: launch-run
spec:
  replicas: 2
  selector:
    matchLabels:
      app: transaction-service
  template:
    metadata:
      labels:
        app: transaction-service
        app.kubernetes.io/part-of: launch-run
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: transaction-service
          image: ghcr.io/launch-run-core/launch.run.transaction-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9004
              protocol: TCP
          envFrom:
            - configMapRef:
                name: launch-run-config
            - secretRef:
                name: launch-run-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 9004
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 9004
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: launch-run-dev
  labels:
    app: transaction-service
    app.kubernetes.io/part-of: launch-run
spec:
  type: ClusterIP
  selector:
    app: transaction-service
  ports:
    - name: http
      port: 9004
      targetPort: 9004
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: transaction-service
  namespace: launch-run-dev
  labels:
    app: transaction-service
    app.kubernetes.io/part-of: launch-run
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: transaction-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
