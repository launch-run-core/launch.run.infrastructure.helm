apiVersion: apps/v1
kind: Deployment
metadata:
  name: campaign-service
  namespace: launch-run-dev
  labels:
    app: campaign-service
    app.kubernetes.io/part-of: launch-run
spec:
  replicas: 2
  selector:
    matchLabels:
      app: campaign-service
  template:
    metadata:
      labels:
        app: campaign-service
        app.kubernetes.io/part-of: launch-run
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: campaign-service
          image: ghcr.io/launch-run-core/launch.run.campaign-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9027
              protocol: TCP
          envFrom:
            - configMapRef:
                name: launch-run-config
            - secretRef:
                name: launch-run-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 9027
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 9027
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: campaign-service
  namespace: launch-run-dev
  labels:
    app: campaign-service
    app.kubernetes.io/part-of: launch-run
spec:
  type: ClusterIP
  selector:
    app: campaign-service
  ports:
    - name: http
      port: 9027
      targetPort: 9027
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: campaign-service
  namespace: launch-run-dev
  labels:
    app: campaign-service
    app.kubernetes.io/part-of: launch-run
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: campaign-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
