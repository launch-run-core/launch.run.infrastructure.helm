apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-hub
  namespace: forja-staging
  labels:
    app: notification-hub
    app.kubernetes.io/part-of: forja
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-hub
  template:
    metadata:
      labels:
        app: notification-hub
        app.kubernetes.io/part-of: forja
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: notification-hub
          image: ghcr.io/forja-platform/forja-notification-hub:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9053
              protocol: TCP
          envFrom:
            - configMapRef:
                name: forja-config
            - secretRef:
                name: forja-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 9053
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 9053
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: notification-hub
  namespace: forja-staging
  labels:
    app: notification-hub
    app.kubernetes.io/part-of: forja
spec:
  type: ClusterIP
  selector:
    app: notification-hub
  ports:
    - name: http
      port: 9053
      targetPort: 9053
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: notification-hub
  namespace: forja-staging
  labels:
    app: notification-hub
    app.kubernetes.io/part-of: forja
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: notification-hub
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
